# 완료 job 검토 → 수정 사항 정리 (2026-08-14)

**상태: Codex 교차 검증 완료 (턴 0591~0594) + Claude 재검증 완료.**
아래는 교차 검증을 반영한 확정본이다. 최초안에서 **3개 항목이 반증**됐고 **결함 2개가 추가**됐다.

생존 코퍼스 주의: `data/jobs` 15건은 **전부** `agent_provider=gpt`/`gpt_runtime=codex`다.
Claude 제공자 job은 TTL로 회수되어 대조군이 없다 (→ B1).

---

## A. 코드 수정

### A0. 크래시가 성공으로 위장된다  ★★ 최우선 · 신규 (Codex 발견, Claude 재현)

- **위치**: `modules/_common.py:1746-1762` (구현) vs `:1523-1534` (계약),
  `modules/rev/analyzer.py:305-331`, `modules/_common.py:1350-1360`.
- **결함**: 계약은 "샌드박스가 안 돌았거나 에이전트가 러너 전에 죽으면 narrative 스캔을 건너뛴다"고
  적혀 있는데, 구현은 `sandbox_result`가 **truthy**이고 `judge_aborted`/`prejudge_blocked`일 때만
  건너뛴다. `sandbox_result is None`(= 아예 안 돎)은 narrative fallback이 **열린다**.
- **재현** (Claude 독립 실행, Codex 결과와 일치):

  ```
  sandbox=None                        -> ['DH{stale_narrative}']  tier='narrative'
  sandbox={'judge_aborted': True}     -> []                       tier=''
  sandbox={'error':'prejudge_blocked'}-> []                       tier=''
  ```

- **실제 피해** — job `0d88d50ad167`:

  ```
  status  = 'finished'                     ← 성공으로 기록됨
  flags   = ['DH{hc42pesrd3Ro_lntagmv0fui1ywb}']
  error   = 'Separator is not found, and chunk exceed the limit'   ← A1 크래시
  turns   = 1                              ← 첫 턴에 죽음
  result.sandbox = None                    ← 샌드박스 0회
  ```

- **자기 세탁 (2차 효과)**: `result.json`이 신뢰 소스 목록에 있어(`_common.py:1350-1360`),
  narrative 티어로 들어간 flag가 result에 기록된 뒤 재스캔하면 **`tier='runner_regex'`**로
  승격된다. Claude 재현:

  ```
  현재 재스캔 -> ['DH{hc42pesrd3Ro_lntagmv0fui1ywb}']  tier='runner_regex'
  ```

  즉 러너가 검증한 flag와 구분이 불가능해진다.
- **수정**: `None`을 일괄 no-run으로 간주하지 말고 명시적 `sandbox_started`/시도 증거를 전달.
  **에이전트 오류 + 미실행 조합에서는 narrative의 terminal 승격을 금지.**
  자기 result 출력을 신뢰 입력으로 재소비하지 않게 할 것.
- **우선순위 근거**: A1 크래시 자체보다 나쁘다 — 크래시는 실패로 보이지만 이건 **성공으로 보인다.**

### A1. codex 하네스가 64 KiB 한 줄에 죽는다  ★ 최우선

- **위치**: `modules/codex_cli.py:494`(`limit=` 없음), `:532`(`readline()`).
- **재발 3건** (Codex가 1건 추가 발견 — 내 최초 집계는 2건):
  `a213e7591fdf` · `98a0718483c9` · `0d88d50ad167`.
  셋 다 `agent.agent_error_type='ValueError'`,
  `agent.agent_error='Separator is not found, and chunk exceed the limit'`,
  traceback이 `_common.py:8545 → codex_cli.py:532 → StreamReader.readline`으로 동일.
  ※ 컨테이너는 **Python 3.12**다 (traceback이 `/usr/local/lib/python3.12/asyncio/streams.py:577`).
- **경계 실측** (Codex): 65535 OK · 65536 OK · **65537 ValueError** · 1 MiB(limit 16 MiB) OK.

- **처방 — 교차 검증으로 두 번 정정됨**:
  - 내 최초안 "ValueError를 잡아 **부분 읽기**": **틀렸다.** 넘친 줄의 내용은 복구 불가다.
  - Codex의 "catch는 복구책이 아니다": **부분적으로 틀렸다.** Claude가 생산 조건
    (개행 없이 limit 초과)을 3.10·3.13에서 실측한 결과:

    ```
    readline 1회차: ValueError: Separator is not found, and chunk exceed the limit   ← 생산 메시지
    readline 2회차: ValueError: Separator is found, but chunk is longer than limit   ← 그 줄의 꼬리
    readline 3회차: b'SECOND_LINE\n'                                                  ← 정상 복귀
    ```

    즉 **스트림은 재동기화된다.** 넘친 줄만 잃고 이후 줄은 정상 파싱된다.
    다만 ValueError가 **줄당 1회가 아니다** — 넘친 양에 따라 여러 번 난다.
  - **확정 처방**: (1) `limit=` 대폭 상향 + (2) ValueError를 **루프에서 반복 허용**하며 재동기화
    (한 번 잡고 마는 구조면 여전히 죽는다). 넘친 줄의 내용을 보존해야 한다면
    `readuntil()` + `LimitOverrunError` drain (버퍼가 보존됨 — Claude 실측으로 100013B 전량 복구 확인).
- **회귀 테스트**: 64 KiB 초과 · hard max 초과 두 사례. 현재 `scripts/test_codex_cli.py`(34 passed)에 없다.

### A2. prejudge가 막는 동안 샌드박스 0회로 ~$717.67을 태운다  ★ 최우선

- **위치**: `modules/_runner.py:1241-1258`, `modules/_common.py:9605-9709`.
- **수치 (Codex가 정정, Claude 재현)**:

  | job | 턴 | 추정 비용 | BLOCKED | redirect | 샌드박스 |
  |---|---|---|---|---|---|
  | `e34c4b63b50b` | 373 | $132.9518 | 4 | 3 | 0회 |
  | `f3a67cd86353` | 406 | $252.2844 | 4 | 3 | 0회 |
  | `eb18a95f4399` | 293 | $332.4297 | 4 | 3 | 0회 |
  | **합계** | **1072** | **$717.6659 → $717.67** | | | **0회** |

  내 최초 표기 `$717.66`은 **절사 오류**였다. `flag_likelihood` 실제 범위도
  **0.01~0.08**이다(내 최초 표기 0.02~0.08).
- **"같은 원인"은 반증됐다**: 세 job은 같은 **게이트 경로**로 끝났을 뿐 실체는 다르다 —
  e34는 죽은 대상/legacy 포트 + 미검증 chain, f3는 죽은 대상/wiring + macro-ID 막다른 길,
  eb는 **살아있는 대상**에서 upstream 검증 후 terminal 막다른 길.
- **"아무것도 안 했다"도 아니다**: `work/`에 탐침 스크립트와 34 KB 결과 JSON이 있다.
  정확한 진술은 **"검증된 샌드박스 실행에 한 번도 도달하지 못했다"**이다.
- **수정**: "BLOCKED 2회 + 샌드박스 0회 → 운영자 에스컬레이션". 누적 턴/추정 비용을 stop 사유에 기록.
- **구현 순서 (Codex 진리표로 확인)**: `_n=1, fl=.02, self-defeat` 에서 `concede=True, escalate=True`로
  **겹친다.** 반드시 **concede 먼저, 에스컬레이션 나중**. 뒤집으면 `unsolvable_by_analysis`가 사라진다.
- **미검증 가정**: 리다이렉트로 구제됐다는 job 2건이 TTL로 소실. Codex가 커밋
  `87e9ad0` 메시지까지 확인했으나 rescue 횟수 기록 없음 → **UNVERIFIABLE**.
  Codex 의견 = 임계 2 + concede-first (첫 리다이렉트는 보존). Claude 동의.
  단 이 위험을 stop 사유에 명시할 것.

### A3. WHY_STOPPED stop_kind 분리  ※ 등급 하향 — 내 주장이 반증됨

- **내 주장은 틀렸다**: "운영자가 run.log를 파헤치기 전에는 두 경우를 구분할 수 없다."
  실제로는 **파일 안에서 구분된다** (Claude 재현):

  ```
  e34c4b63b50b (prejudge 차단, 미실행): '## Last sandbox run' 섹션 없음  1229B
  3919cf964649 (shadow 모드)         : '## Last sandbox run' 섹션 있음  2152B
  ```

  headline만 `Postjudge verdict: unknown`으로 같을 뿐, shadow 쪽에는 exit code와 stdout/stderr가 있다.
- **남는 가치**: 별도 `stop_kind`와 차단 횟수를 **headline에 올리는 것**은 여전히 가독성 개선이다.
  다만 "진단 불가 결함"이 아니라 **개선 사항**으로 등급을 내린다.

### A4. 집계기가 진행 중/재개대기 job에서 죽는다

- **재현됨** (Codex 실행): `scripts/aggregate_hardening.py:824`
  `AggregationError: unresolved failure classification: 2b495752ce97, c09f2a6c48ff`.
  스냅샷 테스트도 같은 두 id로 **41 ok + 1 ERROR**.
- **원인**: population 제외가 module/internal/parent/하드코딩 예외만 보고 **status를 안 본다**
  (`:230-245`). `stopped`는 error·stop이 둘 다 없을 때만 P0로 빠진다(`:630-631`).
- **수정**: nonterminal과 resume-대기를 사유 태그와 함께 population 경계에서 제외.
  그 밖의 미분류는 **계속 loud failure로 유지**.

### A5. 사용량 표시가 codex job 지출을 $0으로 센다

- **사슬 확인됨**: `modules/codex_cli.py:755-764`(`total_cost_usd=None`, 구독은 호출 단위 과금 없음
  — 설계상 정상) → `api/routes/jobs.py:81-94`(`_job_cost`가 `cost_usd`만 읽음) →
  `:337-360`(종료 0원 job을 장부에서 제외) → `:385-400`(`/usage`의 `spent_usd`).
- **코퍼스**: 15건, authoritative 합 **0.0**, estimate 합 **803.1013**.
- **라이브 확인 — Claude가 수행** (Codex는 자기 namespace에서 연결 불가로 UNVERIFIABLE):

  ```
  GET /api/jobs/usage -> 200
     spent_usd = 0.0     in_flight_estimate_usd = 0.0     budget_usd = 0.0
  ```

  ※ 경로는 `/usage`가 아니라 **`/api/jobs/usage`**다 (`/api/usage`는 404).
- **처방 — Codex가 정정**: estimate를 `spent_usd`에 **몰래 합치지 마라.**
  `modules/usage_ledger.py:7-22`가 "서로 다른 단위를 더하지 말라"고 계약으로 명시한다.
  대신 별도 필드(`terminal_unpriced_estimate_usd`)와 completeness flag를 응답할 것.
- **경계**: `modules/_common.py:8392-8428`의 `_maybe_cost_cap()`은 별도 안전 경로다. **건드리지 말 것.**

### A6. flag 후보 노이즈  ※ 내 처방 두 개가 모두 무효

- **관측**: `eb18a95f4399`의 `meta.flag_candidates[0] = 'DH{[^}'` (Claude 재현).
  출처는 run.log의 shell grep `DH{[^}]*}`이고, matcher가 그 접두 `DH{[^}`를 잡았다.
- **내 최초안 "정규식 메타문자 제외"**: 진짜 flag에 `?` `.` `$` `+`가 흔해 **진짜 flag를 버린다.**
- **내 수정안 "닫는 `}` 없는 후보 제외"**: **효과 0.** `DH{[^}`는 이미 `}`로 끝난다
  (Claude 재현: `endswith('}') = True`). 게다가 explicit marker는 brace 없는 형식도 지원하므로
  전역 brace 필터는 오히려 진짜 marker를 버린다.
- **Codex 처방 (채택)**: generic scan에서 `finditer()`로 span/context를 보존하고,
  match 바로 뒤가 `]`+수량자+`}` 인 **정규식 소스 모양일 때만** 신뢰도를 낮춘다.
  explicit `FLAG_CANDIDATE` marker에는 적용하지 않는다.
- ⚠ 플레이스홀더 자동 폐기는 **금지** — 운영자 방침상 큐레이션은 수동.

### A7. 종료된 job에 에이전트 프로세스가 살아 있다  ※ 신규 (Claude 발견, 미검증)

| job | meta 상태 | agent 프로세스 | 마지막 활동 |
|---|---|---|---|
| `d5e018600139` | stopped | 1일 2시간 생존 | run.log 24시간 49분 정지 |
| `c09f2a6c48ff` | stopped | 2시간 14분 생존 | run.log 1시간 43분 정지 |

- 대조: 진행 중인 `2b495752ce97`은 meta 23초 전·run.log 4초 전 갱신 = 정상.
- **아직 Codex 검증을 거치지 않았다.** 원인(정상 대기인지 누수인지)과 처방은 미정.
- 아무 프로세스도 죽이지 않았다 — 판단은 운영자 몫.

---

## B. 운영 설정

### B1. `job_ttl_days=7`이 측정 코퍼스를 파괴한다

- `modules/settings_io.py:52`. S1 베이스라인 20건 중 **생존 0건**.
- 이번 검증에서 실제로 두 번 막혔다: A2의 리다이렉트-구제 job 2건 확인 불가(Codex도 커밋
  메시지까지 뒤졌으나 실패), Claude 제공자 대조군 전멸로 A5의 인과 판정 불가.
- **권고**: TTL은 두되 회수 전에 `events.jsonl`/`meta.json`/`run.log`만 보관 디렉터리로 승격.
- 결정 필요: 운영자.

---

## C. 운영자 결정

### C1. `flag_likelihood < 0.2` 하드컷 — **양측 모두 현행 유지 권고**

- 판정 지점 `modules/_judge.py:1712-1746`, 도입 근거 `modules/_runner.py:1190-1201`.
- Codex 근거: 이번 web 3건은 단순 오판이 아니다 — 죽은 대상, 죽은 terminal,
  미검증 last-mile이 result에 실제로 남아 있다. A2/A3로 반복 횟수와 관측성을 먼저 고치고,
  완화는 shadow/stratified 증거를 모은 뒤 재결정.
- **Claude 동의.** 구현 금지.

---

## 우선순위 (교차 검증 반영)

| 순위 | 항목 | 근거 |
|---|---|---|
| 1 | **A0 + A1** | 같은 크래시 3건, 그중 1건이 `finished`로 위장됐고 flag가 신뢰 티어로 자기 세탁 |
| 2 | A2 + A3 | 1072턴 / $717.67을 샌드박스 0회로 소모 |
| 3 | B1 + A5 | 측정 자료 보존, missing dollar와 zero 분리 |
| 4 | A4 | 현재 라이브 집계·테스트를 막는다 |
| 5 | A6(수정본) | 문맥 기반 de-noise만 |
| 6 | A7 | Codex 검증 대기 |
| — | C1 | 현행 유지 (양측 합의) |

## 교차 검증 요약

- Codex 판정: V1 CONFIRMED · **V2 REFUTED** · V3 UNVERIFIABLE(충돌은 CONFIRMED) ·
  **V4 REFUTED** · V5 UNVERIFIABLE(라이브 GET만) · **V6 REFUTED** · V7 CONFIRMED · **Q1 신규 결함**
- Claude 재검증: V2·V4·V6·Q1 **전부 재현 확인**. V5의 라이브 GET은 Claude가 수행해 해소.
  V1은 Codex 처방의 일부를 **Claude가 실측으로 반증** (스트림은 재동기화된다).
- 읽기 전용 스위트: `test_codex_cli` 34/0 · `test_flag_scan` 50/0 ·
  `test_deterministic_gates` 49/0 · `test_main_usage_row` 53/0 ·
  `test_hardening_aggregator` 41 ok + 1 ERROR(A4).
- 코드 수정 0, 커밋 0. 구현 범위는 운영자 결정 사항.
