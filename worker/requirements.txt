redis==5.2.0
rq==2.0.0
claude-agent-sdk==0.2.87
# Optional direct Responses API fallback; Codex OAuth uses the CLI instead.
openai>=2.0.0,<3
anyio==4.6.2
httpx==0.27.2
pydantic>=2.11
docker==7.1.0
pwntools==4.13.0
# crypto module
gmpy2==2.2.1
sympy==1.13.3
z3-solver==4.13.4.0
pycryptodome==3.21.0
ecdsa==0.19.0
# pwn / rev — gadget hunting
ROPgadget==7.5
# capstone>=5 unlocks CS_ARCH_AARCH64 (the 4.x line lacks ARM64);
# without it ROPgadget --binary <aarch64.so> silently produces 0
# gadgets and the agent falls back to byte-level hand search.
capstone>=5.0,<6
# rev — Windows / PE parsing (Tier A). pefile: PE headers / imports /
# sections / resources / TLS callbacks. dnfile: .NET (managed PE) metadata
# without a full decompiler. Both pure-Python (dnfile depends on pefile), no
# native-wheel conflict with capstone/unicorn — safe in the core resolver.
pefile==2024.8.26
dnfile==0.15.0
# NOTE: angr is installed in a SEPARATE RUN layer (see worker/Dockerfile)
# because angr drags `unicorn==1.0.2rc4` which conflicts with capstone>=5.
# Keeping it out of the main requirements.txt means a bad angr release
# can never break the worker — the conflicting `pip install angr` step
# is `|| true`'d so the rest of the image build proceeds without it.
# libheap (PyPI) — NOT what this comment used to claim. It was described
# here, and in the pwn tool catalogue, as glibc heap inspection (arena
# walking, malloc_chunk parsing, tcache enumeration). PyPI `libheap` is a
# NAME COLLISION with the well-known gdb plugin of that name: the installed
# 0.5 is "a general heap library" whose entire public API is
# ['Heap', 'IndexHeap', 'UF'] plus heapify/peek/pop/push — a priority queue.
# The failure mode was the worst kind: `import libheap` SUCCEEDS, so the
# usual sanity check passes and the agent only discovers there is no
# malloc_chunk parser after spending turns hunting for the API. The
# catalogue claim is gone; glibc heap inspection is gdb + GEF/pwndbg.
# Kept installed only because removing it needs an image rebuild and it is
# harmless — nothing imports it. Drop it at the next rebuild.
libheap==0.5
# web — request shaping + parsing
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
# Async HTTP for race-condition exploits. Concrete incident
# 2026-05-25 (job bfce7f3e0c11): main agent burned ~4s on
# `pip install aiohttp` mid-run because race-push TOCTOU chals
# routinely need many concurrent in-flight requests with
# barrier-synchronized sendall(); requests + threading isn't
# tight enough. Pin matches the SDK transport.
aiohttp==3.10.10
# Local chal-server bring-up for "web-via-ctypes" mix chals
# (Flask + custom .so). Saves another mid-run `pip install`
# round-trip (~3s) every time the agent decides to reproduce
# the server locally to test exploit timing.
flask==2.2.2
werkzeug==2.2.2
# misc / forensic — image + binary inspection from Python
Pillow==11.0.0
python-magic==0.4.27
# numpy — vectorised byte/matrix work. Agents reach for it unprompted on
# GF(2^k) linear algebra (UOV / Rainbow / AES column mixing), bitmap and
# stego carving, and any "reshape this blob and XOR a column" step, where the
# pure-Python loop is 100x slower and much harder to read.
#
# It is here because it was NOT here. numpy was absent from BOTH images, so on
# 2026-08-03 job 46bc4387edff simply ran `pip install numpy` inside worker-2's
# LIVE container — and it persisted. Two days later job 06f3a326d453 drew that
# same slot, found numpy importable, and spent 61 turns and $23.43 building a
# UOV forgery on it; the runner had none, so the solver died on line 33 at
# import with 0 bytes of stdout, having executed none of the attack. worker-1
# never had numpy at all, so which slot a job drew decided what it could use.
#
# MUST be added to runner/Dockerfile in the same change. A library present in
# only one of the two images is the dev/run-parity bug this whole file keeps
# re-learning (gcc headers, cpp, gdb, fpylll, /opt/scaffold — every one of
# those blocks is the same story with a different import).
numpy==2.5.1
