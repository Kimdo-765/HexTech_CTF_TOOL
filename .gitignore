.env
data/
__pycache__/
*.pyc
*.pyo
.venv/
node_modules/
.DS_Store
*.log
.claude/
web-ui/version.json
# Host-specific, generated by start.sh (KVM /dev/kvm passthrough when present).
docker-compose.override.yml

# deploy.sh baseline stamp (machine-local, rewritten on every deploy)
.last_deploy

# Scratch from the dynamic-memory simulation work (flock race proof).
# Kept on disk, never tracked: it contains a FIFO and per-run state.
.flockproof/
